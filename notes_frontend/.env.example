# Angular Notes Frontend - Environment variables (to be set by orchestrator)
# PUBLIC_INTERFACE
# These environment variables are required at build time. The orchestrator should map them into the build environment.
# NG_APP_SITE_URL is used for Supabase email redirect on signup/password reset and must be allowlisted in Supabase.
NG_APP_SUPABASE_URL=
NG_APP_SUPABASE_ANON_KEY=
NG_APP_SITE_URL=
